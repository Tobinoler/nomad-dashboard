# --- Sensitive data ---
# The master workbook is intentionally bundled so the hosted app has data.
# This is safe ONLY because the repo is PRIVATE. Once the Google Sheets
# backend is live, delete the file from the repo and drop the "!" exception.
*.xlsx
!Master Evaluation Sheet.xlsx
*.xls
*.csv

# --- Credentials / secrets: NEVER commit these ---
service_account.json
credentials*.json
*-service-account*.json
.env

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Local run artifacts ---
_server.log
_page.html
*.log

# --- OS / editor ---
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
